http:
  routers:
    docker-template:
      rule: Host(`docker-template.${DOMAIN}`)
      service: docker-template
  services:
    docker-template:
      loadBalancer:
        servers:
          - url: http://192.168.1.12:8081
